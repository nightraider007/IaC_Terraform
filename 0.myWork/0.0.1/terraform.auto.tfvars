
# Core variables - overriding defaults or setting required values
Application_name = "SuperApp"
Environment_name = "staging"
Region           = "eu-west-1"
Input_type       = "yaml"

# Example variables with realistic, clear values for learning
Example_Number  = 30.5 # Could be a timeout in seconds
Example_boolean = true # Enable a feature flag

# List: Ordered, indexable
Example_list = ["apple", "banana", "cherry"]

# Set: Unordered, unique values (will remove duplicates if present)
Example_set = ["apple", "banana", "cherry"]


# Map: Key-value structure
Example_map = {
  first  = "apple"
  second = "banana"
  third  = "cherry"
}

# Tuple: Fixed length, ordered, mixed or uniform types
Example_tuple = ["apple", "12", true] # ["string", "number", "bool"]






/* 
    Object_example = {
    name       = "Bob Smith"
    age        = 28
    is_active  = false
    tags       = ["user", "beta-tester"]
    attributes = {
        department = "QA"
        location   = "Berlin"
        timezone   = "Europe/Berlin"
    }
    } 
*/


